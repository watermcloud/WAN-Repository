Kalau mau analisa, pakai jupyternotebook .ipynb
kalau udah tahap pengembangan aplikasi (swagger) bisa pakai pyhton .py


==2 Apr 2023==
Response data dari load from file lama kemungkinana karna seluruh fungsin regex nya di simpan
dalam 1 fungsi.

Fungsi cleaning

Saran: Bisa di imporve cara penulisan code nya ya.
pastiin setiap baris fungsi regexnya running di .ipynb jangan dimasukin dulu ke .py
dari hasi trace eror, aku belum liat code dibawah ini di .ipynb nya

abusive_words = df_abusive["ABUSIVE"].tolist()
    for word in abusive_words:
        text = re.sub(r'\b{}\b'.format(word), ' ', text, flags=re.IGNORECASE)

 alay_words = df_alay["alay"].tolist()
    # normal_words = df_alay["normal"].tolist()
    # for i in range(len(alay_words)):
    #     text = re.sub(r'\b{}\b'.format(alay_words[i]), normal_words[i], text, flags=re.IGNORECASE)


review akhir: saya udah cek bolak balik, fungi codingan di dalem def text_cleansing(text) masih ada yang eror. makannya response nya muter terus